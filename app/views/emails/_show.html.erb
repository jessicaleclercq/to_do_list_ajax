<br>
<div class="container">
  <div class="row">
  
    <div>
      <p><b>Object: </b><%= email.objet %></p>
      <hr>
      <p><%= email.body %></p>
      <div>
        <%= link_to "Delete", email_path(email.id), class:"btn btn-outline-danger", data:{method: "DELETE"}, remote: true %>
        <%= link_to "Mark as not read", email_path(email.id), id:"btn-read", class:"btn btn-outline-info", data:{method: "PUT"}, remote: true %>
      </div>
    </div>
  </div>
</div>